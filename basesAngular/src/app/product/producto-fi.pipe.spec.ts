import { ProductoFiPipe } from './producto-fi.pipe';

describe('ProductoFiPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoFiPipe();
    expect(pipe).toBeTruthy();
  });
});
